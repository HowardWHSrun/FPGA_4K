name: FPGA detail presentation check
on:
  push:
    branches: [presentation]
    paths:
      - index.html
      - presentation/fpga/**
      - scripts/check_fpga_presentation.mjs
      - scripts/check_fpga_viewer.mjs
      - scripts/check_fpga_review.py
      - hardware/fpga-100t-review/**
      - hardware/fpga-interface-study/**
      - .github/workflows/fpga-detail-check.yml
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: fpga-detail-check
  cancel-in-progress: true
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v6
      - name: Validate syntax and source integrity
        run: |
          node --check presentation/fpga/app.js
          node --check presentation/fpga/slides.js
          node --check presentation/fpga/entry.js
          node --check presentation/fpga/review.js
          node --check presentation/fpga/review-board.js
          node --check presentation/fpga/viewer/viewer.js
          python3 scripts/check_fpga_review.py
          node --check scripts/check_fpga_presentation.mjs
          node --check scripts/check_fpga_viewer.mjs
          python3 scripts/check_docs.py
          python3 scripts/check_hardware.py
          git diff --check
      - name: Check published files match this commit
        run: |
          python3 - <<'PY'
          import os,time,urllib.request
          from pathlib import Path
          paths=['index.html','presentation/fpga/index.html','presentation/fpga/review.js','presentation/fpga/review-board.js','presentation/fpga/review.css','presentation/fpga/review-data.json','presentation/fpga/assets/core-front.svg','presentation/fpga/assets/core-back.svg','presentation/fpga/viewer/index.html','presentation/fpga/viewer/viewer.js','presentation/fpga/viewer/boards.json','presentation/fpga/viewer/vendor/kicanvas.js','hardware/fpga-100t-review/manifest.json','hardware/fpga-interface-study/manifest.json','hardware/fpga-100t-review/hardware/FPGA100T_Minimal.kicad_pcb']
          for attempt in range(18):
              try:
                  for p in paths:
                      req=urllib.request.Request('https://howardwhsrun.github.io/FPGA_4K/'+p+'?verify='+os.environ['GITHUB_SHA'],headers={'Cache-Control':'no-cache'})
                      with urllib.request.urlopen(req,timeout=20) as r:
                          assert r.status==200 and r.read()==Path(p).read_bytes(), p+' not current'
                  print('Live parent and FPGA detail assets verified.');break
              except Exception as e:
                  print(attempt+1,str(e),flush=True)
                  if attempt==17: raise
                  time.sleep(8)
          PY
      - name: Install browser test driver
        run: npm install --no-save --package-lock=false --ignore-scripts playwright@1.56.1
      - name: Test review, downloads, history and system entry
        run: |
          export CHROME_PATH=$(command -v google-chrome || command -v chromium || command -v chromium-browser)
          node scripts/check_fpga_presentation.mjs
          VIEWER_OUTPUT_DIR=fpga-browser-check/native node scripts/check_fpga_viewer.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: fpga-detail-browser-check
          path: fpga-browser-check/
